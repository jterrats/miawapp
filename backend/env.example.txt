# Backend miawapp - igual que salesforce-messaging-auth
PORT=3000

# JWT Issuer - debe coincidir con el Keyset en Salesforce (PascalCase)
JWT_ISSUER=GrgMessagingServer

# ====================================================================
# SALESFORCE OAUTH CLIENT CREDENTIALS (NO credenciales de usuario)
# ====================================================================
# Connected App en Salesforce → Client Credentials Flow

SF_CLIENT_ID=tu_client_id
SF_CLIENT_SECRET=tu_client_secret

# Sandbox:
SF_LOGIN_URL=https://test.salesforce.com
SF_TOKEN_URL=https://test.salesforce.com/services/oauth2/token

# Producción:
# SF_LOGIN_URL=https://login.salesforce.com
# SF_TOKEN_URL=https://login.salesforce.com/services/oauth2/token

# 1. cp env.example.txt .env
# 2. Configura SF_CLIENT_ID y SF_CLIENT_SECRET
# 3. npm run generate-keys (genera private-key.pem, public-key.pem)
# 4. Sube la clave pública a Salesforce Keyset
